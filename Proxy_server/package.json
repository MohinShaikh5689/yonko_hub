{
  "name": "anime-proxy-server",
  "version": "1.0.0",
  "description": "Proxy server for anime streaming API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "ioredis": "^5.6.1",
    "node-fetch": "^2.7.0",
    "redis": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
